{
  "schema": 1,
  "enabled": true,
  "request_id": "one-minute-scheduler-health-20260905-009",
  "recipe": "custom/ba-agent/diagnostics/workflow-strengthening-health.json",
  "note": "Verify the Synology BA lab scheduler is now invoking run_worker_once.sh on an approximately one-minute cadence, authenticated self-refresh remains healthy, the lock prevents overlapping active workers, burst cycles still complete cleanly, scheduler return codes remain clean, and no duplicate execution or semantic evaluation is introduced by the tighter trigger interval. Do not expose secrets."
}
