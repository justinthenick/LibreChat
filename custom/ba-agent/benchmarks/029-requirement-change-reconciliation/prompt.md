Reconcile the baseline requirements against the later evidence packet.

Produce a delta register that preserves both baseline and new provenance, distinguishes explicit Accepted/withdrawn changes from proposals/disputes, does not treat silence as removal, and identifies only the requirement IDs whose downstream artifacts need review.

Do not infer decision authority, promote suggestions into changes, or rewrite the entire baseline.