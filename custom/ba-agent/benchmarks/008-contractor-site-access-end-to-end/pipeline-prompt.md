This is an architecture-comparison run for an end-to-end Business Analyst workflow.

Follow your assigned specialist-agent contract exactly. Preserve all supplied IDs, statuses, uncertainty, authority boundaries and process constraints. Downstream detail must never become more certain than its upstream handoff.

Do not invent UI, validation/error behavior, notifications, workflows, roles/permissions, governance, architecture, APIs, storage, queues, estimates, dates, concrete test data, environments, automation or inspection mechanisms.

Your output will be persisted verbatim and supplied as the only business-analysis artifact to the next specialist stage, so make the handoff complete, traceable and self-contained without adding unsupported detail.
