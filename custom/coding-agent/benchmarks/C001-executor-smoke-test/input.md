# C001 input

The disposable `executor-smoke-test` repository contains:

- `calculator.js` with a working `add(a, b)` function.
- `subtract(a, b)` throws `TODO: implement subtract`.
- `calculator.test.js` contains one passing addition test and one failing subtraction test.
- `package.json` maps `npm test` to Node's built-in test runner.

The source repository must remain unchanged. All edits must occur in a new executor-created task worktree.
